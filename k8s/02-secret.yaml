# NOTE: For a real assignment/production repo, do NOT commit real secrets.
# This is a local-only demo secret for the Kind cluster. In production this
# would come from a secret manager (see DESIGN.md).
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: assignment
type: Opaque
stringData:
  DB_PASSWORD: "S3cureLocalDemoPass!"
  POSTGRES_PASSWORD: "S3cureLocalDemoPass!"
